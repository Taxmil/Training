import { AnimeFilterPipe } from './anime-filter.pipe';

describe('AnimeFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new AnimeFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
